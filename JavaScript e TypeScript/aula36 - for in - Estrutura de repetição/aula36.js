// for in -> lé os indices ou chaves do objeto.

const frutas = ['Pera', 'Maça','Uva'];

for (let i in frutas){
     console.log(frutas[i]);
}

const pessoas = {
     nome: 'joão',
     sobrenome: 'sobrenome',
     idade: 10,
     

}
console.log(pessoas.nome+"⏰");
console.log(pessoas['nome']+'🐱‍👤');